import sys data = sys.stdin.read().strip().split()it = iter(data) def ni():    return int(next(it)) def ns():    return next(it) def solve():    import math    n=ni()      arr=[ni() for _ in range(n)]    ans,diff=0,1    for i in range(1,n):        diff=abs(arr[i]-(i+1))        if ans==0:            ans=diff        else:            ans=math.gcd(ans,diff)                print(ans)    t = ni() for _ in range(t):    solve()