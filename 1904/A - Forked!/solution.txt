import sys data = sys.stdin.read().strip().split()it = iter(data) def ni():    return int(next(it)) def ns():    return next(it) def solve():    a,b= ni(),ni()    kx,ky= ni(),ni()    qx,qy= ni(),ni()    directions=[(a,b),(a,-b),(-a,b),(-a,-b),(b,a),(b,-a),(-b,a),(-b,-a)]    king=set()    quen=set()    count=0    for dx,dy in directions:        king.add((kx+dx,ky+dy))            for dx,dy in directions:        quen.add((qx+dx,qy+dy))    for pos in king:        if pos in quen:            count+=1    print(count)                    t = ni() for _ in range(t):    solve()