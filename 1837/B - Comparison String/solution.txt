import sys data = sys.stdin.read().strip().split()it = iter(data) def ni():    return int(next(it)) def ns():    return next(it) def solve():    n=ni()               s=ns()    current,max_strike=1,1    for i in range(1,n):        if s[i]==s[i-1]:            current+=1            max_strike=max(max_strike,current)        else:            current=1    print(max_strike+1)              t = ni() for _ in range(t):    solve()