import sys data = sys.stdin.read().strip().split()it = iter(data) def ni():    return int(next(it)) def ns():    return next(it) def solve():    n = ni()                     arr = [ni() for _ in range(n)]       ans=float("-inf")    ans=max(ans,arr[-1]-arr[0])    for i in range(1,n):        ans=max(ans,arr[i]-arr[0])    for i in range(0,n-1):        ans=max(ans,arr[-1]-arr[i])    for i in range(n-1):        ans=max(ans,arr[i]-arr[i+1])    print(ans)      t = ni() for _ in range(t):    solve()