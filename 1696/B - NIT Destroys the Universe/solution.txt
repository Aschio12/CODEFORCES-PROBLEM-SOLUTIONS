import sys data = sys.stdin.read().strip().split()it = iter(data) def ni():    return int(next(it)) def ns():    return next(it) def solve():    n = ni()                     arr = [ni() for _ in range(n)]       ans=0    l,r=0,n-1    while l<n and arr[l]==0:        l+=1    while r>=0 and arr[r]==0:        r-=1            if l>r:        print(0)        return     for i in range(l,r+1):        if arr[i]==0:            print(2)            return                print(1)                     t = ni() for _ in range(t):    solve()