import sys data = sys.stdin.read().strip().split()it = iter(data) def ni():    return int(next(it)) def ns():    return next(it) def solve():    from collections import Counter    n = ni()                     arr = [ni() for _ in range(n)]    d=Counter(arr)    if d[2]>0:        if d[2]%2==0:            required=d[2]//2            count=0            for i in range(len(arr)):                if arr[i]==2:                    count+=1                if count==required:                    print( i+1)                    return        else:            print(-1)    else:        print(1)                t = ni() for _ in range(t):    solve()