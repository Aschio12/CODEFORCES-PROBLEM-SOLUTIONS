import sys data = sys.stdin.read().strip().split()it = iter(data) def ni():    return int(next(it)) def ns():    return next(it) def solve():                n = ni()                     arr = [ni() for _ in range(n)]       new_arr=[arr[-1]]+arr[:-1]    prefix=0    ugly=False    for i in range(1,n):        prefix+=new_arr[i-1]        if prefix==new_arr[i]:            ugly=True            break    if ugly:        print("NO")    else:        print("YES")        print(*new_arr)                     t = ni() for _ in range(t):    solve()