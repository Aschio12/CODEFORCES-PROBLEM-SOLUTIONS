import sysimport mathinput = sys.stdin.readlineprint = lambda *args, sep=' ', end='\n': sys.stdout.write(sep.join(map(str, args)) + end) def inn(): return int(input())                       def inl(): return list(map(int, input().split()))    def ins(): return input().split()                    def inm(): return map(int, input().split())            def solve():    a,b=inm()    if a==b:        print(*(0,0))        return    d=abs(a-b)    #we are going to find gcd of (a,d) so it is valid if a is muliple of d    up_a=a%d  #up_a +a is the first multiple with d wich is greater than a    down_a=d-(a%d) #down_a+a is the first multiple of d and a wich is less than a    #since we want to minimize the distance we must take the smallest distance     ans=(d,min(up_a,down_a))    print(*ans)                                     if __name__ == '__main__':    t=inn()    for _ in range(t):        solve()