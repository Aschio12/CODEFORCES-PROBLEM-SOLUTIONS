import sys data = sys.stdin.read().strip().split()it = iter(data) def ni():    return int(next(it)) def ns():    return next(it) def solve():    import math    n,q=ni(),ni()      arr=[ni() for _ in range(n)]    prefix=[arr[0]]    for i in range(1,n):        prefix.append(arr[i]+prefix[i-1])    for _ in range(q):        l,r,x=ni(),ni(),ni()        addened=x*(r-l+1)        if l==1:            removed=prefix[r-1]        else:            removed=prefix[r-1]-prefix[l-2]        total=prefix[-1]-removed+addened        if total%2==0:            print("NO")        else:            print("YES")t = ni() for _ in range(t):    solve()