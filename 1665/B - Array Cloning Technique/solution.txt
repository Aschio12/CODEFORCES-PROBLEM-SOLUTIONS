import sysfrom collections import Counter input = sys.stdin.read().split()ptr = 0 def solve():    global ptr    n = int(input[ptr])    ptr += 1        a = input[ptr : ptr + n]    ptr += n     if n == 1:        print(0)        return     counts = Counter(a)    f = max(counts.values())        if f == n:        print(0)        return     ans = n - f         current_f = f    while current_f < n:        ans += 1          # Add 1 for the Clone operation        current_f *= 2    # Your capacity doubles after each clone + swap            print(ans) t_str = input[0]t = int(t_str)ptr = 1for _ in range(t):    solve()