import sys input = sys.stdin.readline def solve():    n = int(input())        count2 = 0    count3 = 0        # Step 1: Count how many 2s are in the factorization    while n % 2 == 0:        n //= 2        count2 += 1            # Step 2: Count how many 3s are in the factorization    while n % 3 == 0:        n //= 3        count3 += 1            # Step 3: Check for impossibility    # If n > 1, it means there were other prime factors (like 5, 7...)    # If count2 > count3, we can't ever reach 1.    if n > 1 or count2 > count3:        print("-1")    else:        # Step 4: Calculate moves using the formula        # Moves = (missing 2s) + (total divisions by 6)        print((count3 - count2) + count3) if __name__ == '__main__':    t_str = input().strip()    if t_str:        for _ in range(int(t_str)):            solve()