import sys data = sys.stdin.read().strip().split()it = iter(data) def ni():    return int(next(it)) def ns():    return next(it) def solve():    from collections import Counter    n,k= ni(),ni()    s= ns()        count=Counter(s)    even,odd=0,0    for key,val in count.items():        if val%2==0:            even+=val        else:            if val>1:                even+=val-1            odd+=1    req=n-k    if max(count.values())>=req:        print("YES")         return    if req%2==0:        if even>=req:            print("YES")        else:            print("NO")    else:        if even>=req-1 and len(count)>=2:            print("YES")        else:            print("NO")        t = ni()for _ in range(t):    solve()