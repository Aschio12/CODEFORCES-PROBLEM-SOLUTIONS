from bisect import bisect_leftn=int(input())piles=list(map(int,input().split()))q=int(input())queries=list(map(int,input().split()))store=[]start=1for num in piles:    s=[start+num-1,start]    store.append(s[0])    start=s[0]+1       ans=[0]*qfor qu in queries:    left,right=0,len(store)-1    while left<=right:        mid=(left+right)//2        if qu<=store[mid]:            right=mid-1        elif qu>store[mid]:            left=mid+1            print(left+1)