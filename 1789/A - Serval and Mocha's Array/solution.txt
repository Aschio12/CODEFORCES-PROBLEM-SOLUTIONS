import sys data = sys.stdin.read().strip().split()it = iter(data) def ni():    return int(next(it)) def ns():    return next(it) def solve():    import math    found=False                 n = ni()                     arr = [ni() for _ in range(n)]    for i in range(n-1):        for j in range(i+1,n):            if math.gcd(arr[i],arr[j])<=2:                print("Yes")                return           print("No")        t = ni() for _ in range(t):    solve()